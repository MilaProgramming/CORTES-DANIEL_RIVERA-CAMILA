package com.ctd.pruebas;
/*
import com.ctd.modelo.Odontologo;
import org.junit.Before;
import org.junit.Test;
import java.util.List;
import static org.junit.Assert.*;
*/
public class ArrayTest {
/*
    private DaoImplArray dao;

    @Before
    public void setUp() {
        dao = new DaoImplArray();
    }

    @Test
    public void testGuardar() {
        Odontologo odontologo = new Odontologo(1, "Juan", "Pérez");
        Odontologo result = dao.guardar(odontologo);

        assertNotNull("El odontólogo guardado no debe ser nulo", result);
        assertEquals("El ID del odontólogo guardado debe ser 1", Integer.valueOf(1), result.getId());
        assertEquals("El nombre del odontólogo guardado debe ser Juan", "Juan", result.getNombre());
        assertEquals("El apellido del odontólogo guardado debe ser Pérez", "Pérez", result.getApellido());
    }

    @Test
    public void testListarTodo() {
        // Guardar dos odontólogos
        dao.guardar(new Odontologo(1, "Juan", "Pérez"));
        dao.guardar(new Odontologo(2, "Ana", "Gómez"));

        List<Odontologo> odontologos = dao.listarTodo();

        assertNotNull("La lista de odontólogos no debe ser nula", odontologos);
        assertEquals("La lista de odontólogos debe contener dos elementos", 2, odontologos.size());

        // Verificar los detalles de cada odontólogo en la lista
        assertEquals("El primer odontólogo debe ser Juan Pérez", "Juan", odontologos.get(0).getNombre());
        assertEquals("El primer odontólogo debe ser Pérez", "Pérez", odontologos.get(0).getApellido());

        assertEquals("El segundo odontólogo debe ser Ana Gómez", "Ana", odontologos.get(1).getNombre());
        assertEquals("El segundo odontólogo debe ser Gómez", "Gómez", odontologos.get(1).getApellido());
    }
*/
}
